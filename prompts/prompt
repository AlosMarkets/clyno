Good progress, but this is not accepted yet.

Your own verification output still shows bad memory quality:

- Decision: "to use JWT auth."
- Bug/Todo: "fix Redis blacklist bug."

These are still fragments. They must be repaired before storage.

Expected:

- Decision: "Use JWT auth."
- Bug/Todo: "Fix Redis blacklist bug."

Also, if both of these exist:

- "Use JWT auth."
- "Use JWT auth because it is stateless."

Keep only the richer one:

- "Use JWT auth because it is stateless."

Do not claim the memory quality layer is complete until the output is human-readable.

Required fixes:

1. Phrase repair must happen before write and before inject.

Repair rules:
- "to use X" → "Use X."
- "decided to use X" → "Use X."
- "we decided to use X" → "Use X."
- "need to fix X" → "Fix X."
- "fix X" → "Fix X."
- "remaining: add X" → "Add X."
- "it's" → "it is"

2. Capitalization must be fixed.

Examples:
- "fix Redis blacklist bug." → "Fix Redis blacklist bug."
- "to use JWT auth." → "Use JWT auth."

3. Richer-memory dedupe must prefer more complete memories.

If two memories overlap and one has a reason, keep the one with the reason.

Example:
Reject:
- "Use JWT auth."
- "Use JWT auth because it is stateless."

Keep:
- "Use JWT auth because it is stateless."

4. Summaries must not duplicate decisions.

Do not write raw decision text into summaries.md.

5. Re-run these exact tests:

Input:
"We decided to use JWT auth and need to fix Redis blacklist bug!!!"

Expected:
- Decision: "Use JWT auth."
- Todo/Bug: "Fix Redis blacklist bug."

Input:
"We decided to use JWT auth because it's stateless."

Expected:
- Decision: "Use JWT auth because it is stateless."

Input:
"to use JWT auth"

Expected:
- Decision: "Use JWT auth."

Input:
"need to fix Redis blacklist bug"

Expected:
- Todo/Bug: "Fix Redis blacklist bug."

Rejected:
- "jwt"
- "auth"
- "to use JWT"
- "because it is stateless"

After fixing, show the actual outputs of:

clino find "JWT"
clino inject "auth"

Acceptance standard:
No fragments.
No lowercase starts.
No duplicate JWT memories.
No "to use..." memories.
No raw summaries duplicating decisions.
